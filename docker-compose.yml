services:
  noordkaravane:
    image: nginx:alpine
    container_name: noordkaravane
    restart: unless-stopped
    ports:
      - "8260:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./docker/nginx.conf:/etc/nginx/conf.d/default.conf:ro
